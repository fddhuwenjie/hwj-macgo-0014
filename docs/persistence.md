# 持久化与耐久性

所有写入操作遵循以下原则：

1. 写入临时文件。
2. 调用 fsync 强制刷盘。
3. 原子重命名为最终文件。
4. 对目录执行 fsync 确保目录项持久。

仓库层使用 JSON 文件存储实体，乐观锁通过版本号实现。

写前日志 (journal) 记录每个领域事件，采用长度前缀和校验和，保证完整性。

快照包含全量数据快照和校验和，恢复时先校验完整性，再重建目录。
